/* 'use strict';

class User {
  constructor() {
    this.db = [];
  }

  create(name, service) {
    let entry = { 
      name: name,
      service: service,
    };
    this.db.push(entry);
    return entry;
  }
}

module.exports = User;
 */